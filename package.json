{
  "name": "actions-prettier",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "ncc build index.js",
    "check": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@actions/core": "^1.10.1",
    "prettier": "^3.0.3",
    "shell-quote": "^1.8.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
