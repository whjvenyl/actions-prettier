{
  "name": "actions-prettier",
  "version": "0.0.0",
  "scripts": {
    "build": "ncc build index.js",
    "check": "prettier --check .",
    "format": "prettier --write ."
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "prettier": "^2.8.3",
    "shell-quote": "^1.8.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
